# Ядро системы работает на стандартной библиотеке.
# Это сознательно: воспроизводимость — критерий оценки.
PyYAML>=6.0
pandas>=2.0
numpy>=1.24

# Понадобится Person 3 (агент качества)
scikit-learn>=1.3
lightgbm>=4.0        # monotone_constraints по знаку, критично для серы

# Понадобится Person 4 (оптимизация), опционально
scipy>=1.11

# Опционально: тесты и демо
pytest>=7.0
matplotlib>=3.7
